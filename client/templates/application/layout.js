Template.layout.helpers({
  // Page heading/title of each section -- displayed in .page-header (i.e. not the actual <title></title> value)
  pageTitle: function() { return Session.get('pageTitle'); }
});